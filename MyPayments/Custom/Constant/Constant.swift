import Foundation

enum Constant {}

// MARK: - Constant.Theme
extension Constant {
    enum Theme {
        static let selectedTheme = "selectedTheme"
    }
}

// MARK: - Constant.Payment
extension Constant {
    enum Payment {
        static let startingBalance: Decimal = 1000
    }
}
