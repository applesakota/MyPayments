import SwiftUI

extension Color {
    static let appBackground = Color(white: 0.16)

    static let cardGradientStart = Color(white: 0.32)
    static let cardGradientEnd = Color(white: 0.12)

    static let floatingButtonForeground = Color(white: 0.2)
}
